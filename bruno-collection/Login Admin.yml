info:
  name: Login Admin
  type: http
  seq: 15

http:
  method: POST
  url: http://localhost:8080/api/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "correoElectronico": "admin@mitsu.com",
        "contrasena": "admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
