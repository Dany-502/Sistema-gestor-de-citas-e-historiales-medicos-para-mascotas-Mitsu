info:
  name: Login Error
  type: http
  seq: 16

http:
  method: POST
  url: https://mitsu-veterinaria.duckdns.org/api/auth/login
  body:
    type: json
    data: |-
      {
        "correoElectronico": "miguel@mitsucom",
        "contrasena": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
